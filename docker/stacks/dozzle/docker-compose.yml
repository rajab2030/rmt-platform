services:
  dozzle:
    image: amir20/dozzle:v8.13.6
    container_name: dozzle
    restart: unless-stopped

    ports:
      # Access remotely through an SSH tunnel, not an unauthenticated LAN port.
      - "127.0.0.1:8888:8080"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

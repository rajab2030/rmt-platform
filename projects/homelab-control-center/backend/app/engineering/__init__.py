"""RMT-CAP-02 — Engineering Change-Impact & Risk Analysis (above-Core).

Read-only, deterministic, evidence-backed engineering analysis. It never
executes, authorizes, approves, mutates the repository, or modifies
governance. It consumes frozen Core read-only primitives only.
"""

[pytest]
markers =
    e2e: end-to-end test that drives the REAL Docker execution adapter against a
         disposable container. Auto-skips when the Docker daemon is unreachable
         or the base image cannot be obtained, so it is safe in the default
         suite and in CI.
